/**
 * @jsx React.DOM
 */

'use strict';

var React = require('react');

var Form = react.createClass({
  getInitialState: function() {
    return { };
  },
  render: function() {
    return (
      <form>
      </form>
    );
  }
});

module.exports = Form;
